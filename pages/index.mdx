---
seo_title: Explore the Cardano Ecosystem. Find Dapps, Tools, and Updates.
seo_description: Adastack is an open-source index of tools and resources on Cardano. Explore the ecosystem, staking, Dapps, NFTs, Catalyst, governance, and dev tools. 
---

import { CardsIcon, OneIcon, WarningIcon, CardanoLogo } from '@components/icons'
import { Card, Cards } from '../components/cards.js'
import { handleClick } from '../components/handleclick.js'


<br/>
<Cards>
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Intro to Cardano" className="intro-to-cardano-card bg-light-color bg-dark-color" href="#"  />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Staking" className="staking-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="On-Chain" className="on-chain-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Ecosystem" className="ecosystem-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Community" className="community-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="NFTs" className="nft-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Dapps" className="dapps-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Gaming" className="dapps-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Catalyst" className="catalyst-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Governance & DAOs" className="governance-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Development" className="development-card bg-light-color bg-dark-color" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Partner Chains" className="partnerchain-card bg-light-color bg-dark-color" href="#" />
</Cards>