import { CardsIcon, OneIcon, WarningIcon, CardanoLogo } from '@components/icons'
import { Card, Cards } from '../components/cards.js'
import { handleClick } from '../components/handleclick.js'


<br/>


<Cards>
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Intro to Cardano" className="intro-to-cardano-card bg-emerald-50" href="#"  />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="On-Chain" className="on-chain-card bg-cyan-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Staking" className="staking-card bg-sky-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Ecosystem" className="ecosystem-card bg-blue-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="NFTs" className="nft-card bg-indigo-50 bg-violet-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Dapps" className="dapps-card bg-purple-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Community" className="community-card bg-pink-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Development" className="development-card bg-red-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Governance" className="governance-card bg-orange-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Catalyst" className="catalyst-card bg-amber-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="DAOs" className="dao-card bg-lime-50" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Sidechains and L2s" className="sidechain-and-l2-card bg-green-50" href="#" />
</Cards>