import { CardsIcon, OneIcon, WarningIcon, CardanoLogo } from '@components/icons'
import { Card, Cards } from '../components/cards.js'
import { handleClick } from '../components/handleclick.js'


# Home

Welcome to adastack! This is a collection of Cardano tools and resources.

<Cards>
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Intro to Cardano" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="On-Chain" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Ecosystem" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Staking" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="NFTs" href="#" />
  <Card onClick={() => handleClick(event)}  icon={<CardsIcon />} title="Community" href="#" />
</Cards>