---
seo_title: Explore Cardano
# Leave the description blank on the index page so it uses the standard one, defined in theme.config.tsx
seo_description:
---

import {
  CardsIcon,
  OneIcon,
  WarningIcon,
  CardanoLogo,
} from "@components/icons";
import * as THREE from "three";
import { Card, Cards } from "../components/cards.js";
import { CubeAnimation } from "../components/cubeanimation.js";
import { handleCardClick } from "../scripts/handleCardClick.js";

<CubeAnimation />

<Cards>
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Intro to Cardano"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Staking"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="On-Chain"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Ecosystem"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Community"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Dapps"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="NFTs"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Gaming"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Catalyst"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Governance"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="DAOs"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Development"
    className="bg-light-color bg-dark-color"
    href="#"
  />
  <Card
    onClick={() => handleCardClick(event)}
    icon={<CardsIcon />}
    title="Side Chains"
    className="bg-light-color bg-dark-color"
    href="#"
  />
</Cards>
